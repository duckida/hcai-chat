/** @type {import('tailwindcss').Config} */
module.exports = {
  // Tailwind v4 auto-detects sources; this file only carries theme tweaks.
  theme: {
    extend: {
      colors: {
        neutral: {
          950: "#0a0a0a",
        },
      },
    },
  },
  plugins: [],
  darkMode: "class",
};
